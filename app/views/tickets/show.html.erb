<p style="color: green"><%= notice %></p>

<%= render @ticket %>

<div>
  <%= link_to "Edit this ticket", edit_ticket_path(@ticket) %> |
  <%= link_to "Back to All Tickets", tickets_path %> |
  <%= link_to "Back to Home Page", home_index_path %>

  <%= button_to "Cancel this ticket", @ticket, method: :delete %>
</div>
